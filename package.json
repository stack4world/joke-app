{
  "name": "joke-server",
  "version": "1.0.0",
  "description": "Bastion App Server",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node app.js",
    "start:client": "cd client && npm run start",
    "deploy": "echo \"Error: no deploy specified\" && exit 1",
    "postinstall": "cd client && npm install"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.27.2",
    "body-parser": "^1.20.0",
    "dotenv": "^16.0.0",
    "express": "^4.18.1",
    "path": "^0.12.7"
  }
}
